from config.config import Config

class Exp:
    # Generates a randomized list of n words from the provided word bank.
    def generate_word_list(n, word_bank):
        return

    # Sequentially displays each word in the list to the user for a fixed duration.
    def display_words(word_list):
        return

    # Collects user input of recalled words after the word list has been shown.
    def collect_recall():
        return

    # Compares recalled words to the original list and computes recall accuracy by position.
    def analyze_recall():
        return